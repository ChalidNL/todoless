# Todoless Environment Configuration
# Copy this to .env and fill in your values

# JWT Secret for authentication (generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET=change-this-to-a-secure-random-string

# CORS Origin - URL where frontend is accessed
CORS_ORIGIN=http://localhost:3000

# Vite API URL - URL where backend API is accessed  
VITE_API_URL=http://localhost:4000
