/.claude
# Development changelogs/history
# (hidden for end users, only for dev/test)
changes/
# TEST-ONLY: Exclude reset scripts
reset-todoless.bat
# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Build output
dist/
build/
*.local

# Environment variables
.env
.env.local
.env.*.local
# Allow .env.example files for documentation
!.env.example
!server/.env.example

# Secrets (do NOT commit tokens)
secrets/

# IDE
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
logs/
*.log
coverage/
*.tmp
*.bak

# Database files (NEVER commit databases - use volumes)
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3
*.sqlite-journal
!server/data/.keep

# Docker volume path
server/data/
server/public/

# Temporary files
tmp/
temp/
*.tmp

# Coverage
coverage/
.nyc_output/

# TypeScript
*.tsbuildinfo

# Docker Compose overrides (environment-specific, not committed)
docker-compose.*.yml
docker-compose.override.yml
!docker-compose.yml  # Keep only canonical production compose

# Examples/templates folder (local only, not committed)
examples/

# Documentation (except README)
*.md
!README.md

# Release notes (local only)
changes/
public/changes/

# Ignore local instructions and bugs
instruction.md

# Local-only batch scripts (dev/test only, not for production)
boot-todoless.bat
start-todoless-local.bat
reset-todoless.bat

# Archive (do not push - review before removal)
# package-lock.json (analyze and clean up, archive for review)
# nginx.conf (review if needed for production deployment)

# Temporary files
nul
